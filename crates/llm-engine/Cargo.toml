[package]
name = "llm-engine"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1"
kv-registry = { path = "../kv-registry" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[build-dependencies]
bindgen = "0.71"
